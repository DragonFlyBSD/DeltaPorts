{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "drop_patch",
  "description": "Declare an existing patch obsolete and remove it.",
  "type": "object",
  "required": ["type", "target", "reason"],
  "additionalProperties": false,
  "properties": {
    "type": {"const": "drop_patch"},
    "target": {"type": "string", "minLength": 1,
               "description": "Relpath of the patch to drop, or the patch-apply target in overlay.dops"},
    "reason": {"type": "string", "minLength": 1,
               "description": "Why this patch is obsolete — lands in the intent log and the operator commit message"}
  }
}
