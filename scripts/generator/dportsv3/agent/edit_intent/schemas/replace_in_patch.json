{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "replace_in_patch",
  "description": "Edit a single hunk context inside an existing patch file. The most common drift fix.",
  "type": "object",
  "required": ["type", "target", "find", "replace"],
  "additionalProperties": false,
  "properties": {
    "type": {"const": "replace_in_patch"},
    "target": {"type": "string", "minLength": 1,
               "description": "Relpath under ports/<origin>/, e.g. 'dragonfly/patch-src_foo.c'"},
    "find": {"type": "string", "minLength": 1,
             "description": "Exact substring to replace"},
    "replace": {"type": "string",
                "description": "Replacement text (may be empty)"},
    "occurrence": {"type": "integer", "minimum": 1, "default": 1,
                   "description": "Which match to replace if find is non-unique (1-based)"},
    "scope": {"enum": ["@any", "@current"], "default": "@any",
              "description": "Target build line for this op. '@any' (default) applies universally on every build. '@current' resolves to the env's current target at apply time — use for build-line-specific fixes. Never type literal '@2026Q2'; the runner injects t.target."}
  }
}
