--- Makefile.orig	2013-10-13 18:24:25.972007000 +0000
+++ Makefile
@@ -20,6 +20,8 @@ SRC=		${.CURDIR}/src
 
 PLIST_FILES=	bin/portlint \
 		man/man1/portlint.1.gz
+USES+=		shebangfix
+SHEBANG_FILES=	*.pl
 
 .include <bsd.port.options.mk>
 
@@ -31,18 +33,17 @@ PLIST_DIRS=	%%EXAMPLESDIR%%
 do-fetch:
 	@${DO_NADA}
 
-pre-patch:
-	@${CP} -R ${SRC} ${WRKDIR}
+do-extract:
+	@${MKDIR} ${WRKSRC}
+	@${CP} ${SRC}/* ${WRKSRC}/
 
 post-patch:
-	@${REINPLACE_CMD} -e 's|/usr/bin/perl|${PERL}|' ${WRKSRC}/portlint.pl
-	@${REINPLACE_CMD} -e 's|/usr/bin/perl|${PERL}|' \
-		${WRKSRC}/portlintgrep.pl
+	@${REINPLACE_CMD} -e 's|/usr/ports|/usr/dports|g' ${WRKSRC}/portlint*
 
 do-install:
 	${INSTALL_SCRIPT} ${WRKSRC}/portlint.pl \
 	    ${STAGEDIR}${PREFIX}/bin/portlint
-	${INSTALL_MAN} ${WRKSRC}/portlint.1 \
+
 	    ${STAGEDIR}${MAN1PREFIX}/man/man1
 .if ${PORT_OPTIONS:MEXAMPLES}
 	${INSTALL} -o ${SHAREOWN} -g ${SHAREGRP} -m ${BINMODE} -d \
