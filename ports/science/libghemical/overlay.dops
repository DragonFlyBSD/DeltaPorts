port science/libghemical
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk eval BUILD_DEPENDS "${BUILD_DEPENDS:N*binutils}"
mk eval CONFIGURE_ENV "${CONFIGURE_ENV:NLD_PRELOAD*}"
