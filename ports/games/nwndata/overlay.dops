port games/nwndata
type port
reason "set IGNORE for linux-only data port + manual package build guard"
target @any

mk set IGNORE "data for game that only runs on linux emulator"

mk block set condition "!defined(DPORTS_BUILDER)" <<'MK'
MANUAL_PACKAGE_BUILD=	"huge" package (too big for mirrors)
MK
