--- CMakeLists.txt.orig	2026-09-05 02:06:54 UTC
+++ CMakeLists.txt
@@ -28,7 +28,7 @@ elseif(CMAKE_SYSTEM_NAME MATCHES "Android")
     cm_define_var(VDE_BIONIC 1)
 elseif(CMAKE_SYSTEM_NAME MATCHES "Darwin")
     cm_define_var(VDE_DARWIN 1)
-elseif(CMAKE_SYSTEM_NAME MATCHES "FreeBSD")
+elseif(CMAKE_SYSTEM_NAME MATCHES "FreeBSD|DragonFly")
     cm_define_var(VDE_FREEBSD 1)
 else()
     message(FATAL_ERROR "Unsupported OS: ${CMAKE_SYSTEM_NAME}")
@@ -250,9 +250,13 @@ if(ENABLE_TUNTAP)
 	    set(ENABLE_TUNTAP OFF)
         endif()
 
-    elseif(CMAKE_SYSTEM_NAME MATCHES "FreeBSD")
+    elseif(CMAKE_SYSTEM_NAME MATCHES "FreeBSD|DragonFly")
         include(CheckIncludeFile)
-        cm_check_include("net/if.h;net/if_tun.h" HAVE_NET_IF_TUN_H)
+        if(CMAKE_SYSTEM_NAME MATCHES "DragonFly")
+            cm_check_include("net/if.h;net/tun/if_tun.h" HAVE_NET_IF_TUN_H)
+        else()
+            cm_check_include("net/if.h;net/if_tun.h" HAVE_NET_IF_TUN_H)
+        endif()
         if(HAVE_NET_IF_TUN_H)
             add_definitions(-DHAVE_TUNTAP=1)
         else()
