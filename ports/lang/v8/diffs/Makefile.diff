--- Makefile.orig	2013-05-12 10:44:56.539975000 +0000
+++ Makefile
@@ -16,21 +16,13 @@ USE_XZ=		yes
 USE_PYTHON=	2.6+
 USE_LDCONFIG=	yes
 ALL_TARGET=	native
-MAKE_ARGS=	library=shared
+MAKE_ENV+=	GYPFLAGS=-DOS=freebsd
+MAKE_ENV+=	library=shared
+USE_GCC=	4.7
+CFLAGS+=	-D__FreeBSD__=1
 
 .include <bsd.port.pre.mk>
 
-.if ${CC:T:Mclang} == "clang" || ${CXX:T:Mclang++} == "clang++" || ${OSVERSION} >= 1000024
-_CLANG!=	clang --version | ${HEAD} -1 | ${SED} -e 's/.*clang version \([0-9]\)\.\([0-9]\).*/\1\2/'
-MAKE_ENV+=	LINK=clang++
-CFLAGS+=	-Wno-unused-private-field
-.if ${_CLANG} >= 33
-CFLAGS+=	-Wno-nested-anon-types
-.endif
-.else
-MAKE_ARGS+=	strictaliasing=off
-USE_GCC=	any
-.endif
 
 ONLY_FOR_ARCHS=	i386 amd64
 
