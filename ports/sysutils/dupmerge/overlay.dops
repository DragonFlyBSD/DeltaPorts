port sysutils/dupmerge
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk set LDFLAGS "${LDFLAGS:N-lroken}"
mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e '/roken.h/d'	\
		${WRKSRC}/dupmerge.c
MK1
