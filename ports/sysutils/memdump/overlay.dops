port sysutils/memdump
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e 's@\(FreeBSD\.*\)@DragonFly.*|\1@g'	\
		${WRKSRC}/makedefs
MK1
