--- config.h.meson.orig
+++ config.h.meson
@@ -49,6 +49,15 @@
 /* Building with logind */
 #mesondefine HAVE_LOGIND
 
+/* Defined if libseat (seatd) is used for seat/session management */
+#mesondefine HAVE_LIBSEAT
+
+/* Defined if a launcher backend (logind or libseat) is available */
+#mesondefine HAVE_LAUNCHER
+
+/* Defined if libinput has the plugin system (libinput >= 1.30) */
+#mesondefine HAVE_LIBINPUT_PLUGIN_SYSTEM
+
 /* Define if you want to enable the native (KMS) backend based on systemd */
 #mesondefine HAVE_NATIVE_BACKEND
 
