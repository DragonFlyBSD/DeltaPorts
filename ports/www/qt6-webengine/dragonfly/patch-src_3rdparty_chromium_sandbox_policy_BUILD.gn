--- src/3rdparty/chromium/sandbox/policy/BUILD.gn.intermediate	2026-09-17 07:06:57 UTC
+++ src/3rdparty/chromium/sandbox/policy/BUILD.gn
@@ -136,6 +136,16 @@ component("policy") {
       "//ui/gfx/x",
     ]
   }
+  if (is_dragonfly) {
+    sources += [
+      "dragonfly/sandbox_dragonfly.cc",
+      "dragonfly/sandbox_dragonfly.h",
+    ]
+    deps += [
+      "//sandbox:sandbox_buildflags",
+      "//ui/gfx/x",
+    ]
+  }
   if (is_chromeos) {
     sources += [
       "linux/bpf_ime_policy_linux.cc",
