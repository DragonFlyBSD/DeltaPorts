--- src/3rdparty/chromium/v8/BUILD.gn.intermediate	2026-09-17 08:38:25 UTC
+++ src/3rdparty/chromium/v8/BUILD.gn
@@ -4756,7 +4756,7 @@
       # iOS Xcode simulator builds run on an x64 target. iOS and macOS are both
       # based on Darwin and thus POSIX-compliant to a similar degree.
       if (is_linux || is_chromeos || is_mac || is_ios ||
-          target_os == "freebsd") {
+          target_os == "freebsd" || target_os == "dragonfly") {
         sources += [ "src/trap-handler/handler-inside-posix.h" ]
       } else if (is_win) {
         sources += [ "src/trap-handler/handler-inside-win.h" ]
@@ -6213,7 +6213,7 @@
       # iOS Xcode simulator builds run on an x64 target. iOS and macOS are both
       # based on Darwin and thus POSIX-compliant to a similar degree.
       if (is_linux || is_chromeos || is_mac || is_ios ||
-          target_os == "freebsd") {
+          target_os == "freebsd" || target_os == "dragonfly") {
         sources += [
           "src/trap-handler/handler-inside-posix.cc",
           "src/trap-handler/handler-outside-posix.cc",
@@ -6893,7 +6893,7 @@
       "src/base/platform/platform-openbsd.cc",
     ]
     libs = [ "execinfo" ]
-  } else if (is_freebsd) {
+  } else if (is_freebsd || is_dragonfly) {
     sources += [
       "src/base/debug/stack_trace_posix.cc",
       "src/base/platform/platform-freebsd.cc",
