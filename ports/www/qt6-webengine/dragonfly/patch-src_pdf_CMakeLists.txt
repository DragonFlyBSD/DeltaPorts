--- src/pdf/CMakeLists.txt.intermediate	2024-05-27 23:30:19 UTC
+++ src/pdf/CMakeLists.txt
@@ -71,7 +71,7 @@ add_dependencies(prepare_docs_Pdf genera
 #   TOOLCHAIN SETUP
 ##
 
-if(LINUX OR MINGW OR FREEBSD)
+if(LINUX OR MINGW OR FREEBSD OR DRAGONFLY)
     setup_toolchains()
 endif()
 
@@ -134,7 +134,7 @@ foreach(arch ${archs})
           strip_absolute_paths_from_debug_symbols=false
         )
 
-        if(LINUX OR ANDROID OR FREEBSD)
+        if(LINUX OR ANDROID OR FREEBSD OR DRAGONFLY)
             list(APPEND gnArgArg
                 is_cfi=false
                 ozone_auto_platforms=false
