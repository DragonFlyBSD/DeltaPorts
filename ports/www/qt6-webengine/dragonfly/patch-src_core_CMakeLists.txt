--- src/core/CMakeLists.txt.intermediate	2026-09-17 08:38:25 UTC
+++ src/core/CMakeLists.txt
@@ -21,7 +21,7 @@ add_subdirectory(tools/qwebengine_convert_dict)
 #   TOOLCHAIN SETUP
 ##
 
-if(LINUX OR WIN32 OR FREEBSD)
+if(LINUX OR WIN32 OR FREEBSD OR DRAGONFLY)
     setup_toolchains()
 endif()
 
@@ -452,7 +452,7 @@ foreach(arch ${archs})
             CONDITION QT_FEATURE_webenginedriver
         )
 
-        if(LINUX OR FREEBSD)
+        if(LINUX OR FREEBSD OR DRAGONFLY)
             list(APPEND gnArgArg
                 use_gtk=false # GTK toolkit bindings
                 use_qt5=false # Qt5 toolkit bindings
