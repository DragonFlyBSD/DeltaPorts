port devel/rapidjson
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk eval OPTIONS_DEFAULT "${OPTIONS_DEFAULT:NDOXYGEN}"
mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e 's/-Werror[[:>:]]//g' ${WRKSRC}/CMakeLists.txt
MK1
