port devel/rubygem-cucumber-core
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e '/cucumber-tag_expressions>/s@~>@>=@g' \
		${WRKSRC}/cucumber-core.gemspec
MK1
