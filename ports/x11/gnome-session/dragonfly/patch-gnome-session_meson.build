--- gnome-session/meson.build.orig	2026-07-04 14:08:37 UTC
+++ gnome-session/meson.build
@@ -21,8 +21,12 @@ executable(
 
 sources = files(
     'gsm-util.c',
-    'leader-systemd.c',
 )
+if enable_systemd
+  sources += files('leader-systemd.c')
+else
+  sources += files('leader-dragonfly.c')
+endif
 
 executable(
   meson.project_name() + '-init-worker',
@@ -45,10 +49,13 @@ sources = files(
   'gsm-shell.c',
   'gsm-store.c',
   'gsm-system.c',
-  'gsm-systemd.c',
   'gsm-util.c',
   'service-main.c'
 )
+
+if enable_systemd
+  sources += files('gsm-systemd.c')
+endif
 
 dbus_ifaces = [
   'org.gnome.SessionManager',
