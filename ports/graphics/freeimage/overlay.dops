port graphics/freeimage
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk add DOS2UNIX_FILES "Makefile.gnu"
mk add CFLAGS "-DZ_HAVE_UNISTD_H"
mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e "/-shared -Wl/s/$$/ -lm/g"	\
		${WRKSRC}/Makefile.gnu
MK1
