--- Makefile.orig	2013-08-10 09:49:47.853932000 +0000
+++ Makefile
@@ -30,9 +30,11 @@ MAN3=		libgphoto2.3 libgphoto2_port.3
 
 .include <bsd.port.options.mk>
 
-.if ${OSVERSION} < 1000040
+.if ${OPSYS} == FreeBSD && ${OSVERSION} < 1000040
 CONFIGURE_ENV+=	LIBUSB_CFLAGS="-I/usr/include" \
 		LIBUSB_LIBS="-lusb"
+.elif ${OPSYS} == DragonFly
+USES+=		usb
 .else
 # Safe because it's never used
 CFLAGS+=	-DENODATA=0
