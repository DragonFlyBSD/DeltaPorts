port security/pkcs11-tools
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk add CFLAGS "-I${OPENSSLINC}"
mk add LDFLAGS "-L${OPENSSLLIB}"
mk add MAKE_ARGS "LIBCRYPTO_LIBS=\"${OPENSSLBASE}/lib/libssl.a ${OPENSSLBASE}/lib/libcrypto.a\""
