port security/pam-mysql
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk eval CONFIGURE_ARGS "${CONFIGURE_ARGS:N--with-pam=*} --with-pam=/usr"
mk add USES "localbase:ldflags"
