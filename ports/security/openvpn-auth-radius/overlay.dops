port security/openvpn-auth-radius
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk target set dfly-patch <<'MK1'
	${REINPLACE_CMD} -e 's@-Wall@& -Wno-deprecated-declarations@g' \
		${WRKSRC}/Makefile.bsd
MK1
