port math/vowpal_wabbit
type port
reason "auto-converted from compat (Makefile.DragonFly + dragonfly/)"
target @any

mk add USES "alias"
file materialize dragonfly/patch-configure -> dragonfly/patch-configure
