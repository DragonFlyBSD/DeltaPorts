port deskutils/znotes
type port
reason "auto-converted from Makefile.DragonFly"
target @any

mk add USES "gmake"
mk target set dfly-install <<'MK1'
	-${MKDIR} ${STAGEDIR}${DATADIR}/translations
	-${CP} -v ${WRKSRC}/translations/znotes_*.qm ${STAGEDIR}${DATADIR}/translations/
MK1
