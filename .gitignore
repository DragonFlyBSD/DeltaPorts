# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
scripts/tools/dev-env/.venv/
env/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# DPorts specific
dports.toml
state/
*.log

# Operator-local config copies. The ``.sample`` files in config/
# are tracked templates; operators copy them locally and edit. The
# loaders fall back to ``.sample`` when the local copy is absent
# so a fresh checkout works out of the box.
config/agentic-policy.json
config/delivery.toml
config/delivery.token

# OS
.DS_Store
Thumbs.db
